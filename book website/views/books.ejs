<%- include("header") -%>

    <h1>Books</h1>
    <form class="" action="/books" method="post">
        <button type="submit" name="add" value=<%= username %>>Add!</button>

        <%  for(let i=0; i<books.length; i++){  %>
            <div class="">
            
                <h3 ><%= books[i].name %></h3>

                <button class="" type="submit" name="edit" value= <%= books[i]._id %> >edit</button>
                <button class="" type="submit" name="delete" value= <%= books[i]._id %> >delete</button>
                <br />
                <label><%= books[i].details%></label>
                <br class="line" />
     
            </div>
            <bn />
            <%  }  %>


    
      
    </form>

<%- include("footer") -%>